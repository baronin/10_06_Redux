import { useState } from "react";

const initialCourse = {
  title: "",
};

const CoursesPage = () => {
  return (
    <div className="container mt-5">
      <h1>Courses Page</h1>
    </div>
  );
};

export default CoursesPage;
